// This file's data is temporarily unused while the periodic table component is being reworked.
export interface Element {
  name: string;
  symbol: string;
  number: number;
  atomic_mass: number;
  category: string;
  xpos: number;
  ypos: number;
}

export const elements: Element[] = [];
